# Session information

This folder stores R `sessionInfo()` logs from successful reproduction runs.

The logs are included so that readers can check the R version and package
versions used during object reconstruction and core analyses.
